<%- include('../template/head') -%>
<div><%= message %></div>
<div>
    <ul>
        <% items.forEach( task => { %>
            <% if(task.title === 'Rattlesnake, horned') { %>
                <li>Hola mundo!</li>
            <% }else{ %>
                <li><%= task.title %></li>
            <% } %>
        <% }); %>
    </ul>
</div>
<%- include('../template/footer') -%>